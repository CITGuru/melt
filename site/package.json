{
  "name": "@melt/site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "getmelt.com landing page + Vercel serverless functions (audit-share endpoint).",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.0",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@vercel/node": "^5.1.10",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0"
  }
}
